# Документация API

Здесь описаны все доступные HTTP-маршруты и структуры данных для алгоритмов, интегрированных в проект.

## Базовые маршруты (System)

- `GET /health` или `GET /api/health`
  Проверка работоспособности сервиса.
  Ответ: `{"status": "ok", "time": "..."}`

- `GET /api/ready`
  Проверка готовности сервиса.
  Ответ: `{"status": "ready"}`

- `GET /api/algorithms`
  Возвращает список всех зарегистрированных алгоритмов с их базовой схемой.
  Ответ: `[{"id": "ahp", "name": "...", "description": "...", "input_schema": {...}, "available": true}, ...]`

---

## Работа с алгоритмами (Runs & Reports)

Общий процесс работы с большинством алгоритмов (кроме нечёткой логики и деревьев решений, для которых есть отдельные методы, см. ниже) предполагает создание прогона (Run) через POST. Результатом является UUID прогона, по которому позже можно получить отчёт или сырой результат.

### Запуск алгоритма
`POST /api/runs`
Тело запроса:
```json
{
  "algorithm_id": "string (например, 'ahp')",
  "input": { 
     // Специфично для алгоритма, см. ниже
  }
}
```
Ответ: `{"run_id": "uuid", "report_url": "..."}`.

### Получение результатов
- `GET /api/runs/<run_id>`
  Возвращает сырой JSON-ответ прогона, включающий входы, выходы и метадату.

- `GET /api/reports/<run_id>`
  Возвращает сгенерированный Markdown-отчёт, описывающий процесс вычислений, графики и итоговое решение.

- `GET /api/reports/<run_id>/csv`
  Скачивание отчёта в формате CSV (если применимо).

---

## Схемы входных данных для `/api/runs`

### Метод анализа иерархий (`ahp`)
```json
{
  "criteria": ["Цена", "Качество", "Скорость"],
  "alternatives": ["Вариант 1", "Вариант 2"],
  "matrix": [
    [1.0, 3.0, 5.0],
    [0.33, 1.0, 2.0],
    [0.2, 0.5, 1.0]
  ],
  "alt_matrices": {
    "Цена": [[1.0, 0.5], [2.0, 1.0]],
    "Качество": [[1.0, 2.0], [0.5, 1.0]],
    "Скорость": [[1.0, 1.0], [1.0, 1.0]]
  }
}
```

### Многокритериальная оптимизация (`multi_criteria`)
```json
{
    "criteria": [
        {"name": "Прибыль", "weight": 0.5, "func_type": "linear", "direction": "max", "params": {"min": 10, "max": 100}},
        {"name": "Расходы", "weight": 0.5, "func_type": "quadratic", "direction": "min", "params": {"ideal": 0, "max_val": 50}}
    ],
    "constraints": {
        "Прибыль": {"min": 20, "max": 90}
    },
    "main_criterion": "Прибыль", // (опционально)
    "cases": {
        "Проект А": {"Прибыль": 50, "Расходы": 10},
        "Проект Б": {"Прибыль": 80, "Расходы": 30}
    }
}
```

### Парные матричные игры (`pair_games`)
```json
{
    "player1_name": "Игрок 1",
    "player2_name": "Игрок 2",
    "player1_strategies": ["Стратегия 1", "Стратегия 2"],
    "player2_strategies": ["Стратегия A", "Стратегия B"],
    "payoff_matrix": [
        [3.0, -1.0],
        [-2.0, 4.0]
    ]
}
```

### Игры с природой (Принятие решений в условиях неопределенности) (`nature_games`)
```json
{
    "decision_maker": "ЛПР",
    "strategies": ["Действие 1", "Действие 2"],
    "states_of_nature": ["Состояние 1", "Состояние 2"],
    "payoff_matrix": [
        [10.0, 20.0],
        [30.0, 5.0]
    ],
    "optimization": "max", // или "min"
    "lambda": 0.5,         // Для критерия Гурвица (0-1)
    "probabilities": [0.4, 0.6] // Для критерия Байеса, сумма должна быть 1.0 (опционально)
}
```

### Алгоритмическое дерево решений ID3 (`id3`)
```json
{
    "features": ["Погода", "Температура", "Влажность"],
    "target": "Играть",
    "data": [
        ["Солнечно", "Жарко", "Высокая", "Нет"],
        ["Пасмурно", "Холодно", "Нормальная", "Да"]
    ]
}
```

---

## Прямые маршруты (Без БД и Отчетов)

Для некоторых алгоритмов предусмотрены "короткие" маршруты, сразу возвращающие результаты вычислений без сохранения в БД.

### Нечёткие множества (Задача 1: Функции принадлежности)
`POST /api/fuzzy/task1`
```json
{
    "concept": "Температура",
    "terms": ["Холодно", "Нормально", "Жарко"], // Строго 3 терма 
    "x_min": 0.0,
    "x_max": 40.0,
    "step": 5.0,
    "x0": 25.0,
    "params": {
        "a1": {"a": 10, "b": 15, "c": 2},
        "a2": {"a": 20, "b": 25, "c": 3},
        "a3": {"a": 30, "b": 35, "c": 4}
    }
}
```

### Нечёткие множества (Задача 2: Композиция)
`POST /api/fuzzy/task2`
```json
{
    "candidates": ["Кандидат 1", "Кандидат 2"],
    "characteristics": ["Характеристика 1", "Характеристика 2"],
    "specialties": ["Специальность 1", "Специальность 2"],
    "R1": [ // Характеристики -> Специальности 
        [0.8, 0.2],
        [0.4, 0.9]
    ],
    "R2": [ // Характеристики -> Кандидаты
        [0.9, 0.1],
        [0.3, 0.7]
    ]
}
```

### Решающее дерево (if-else классификатор)
`POST /api/decision-tree/classify`
```json
{
    "thresholds": {"x1": 60.0, "x2": 70.0, "x3": 65.0},
    "candidates": [
        {"name": "Иванов", "x1": 80, "x2": 50, "x3": 90},
        {"name": "Петров", "x1": 40, "x2": 80, "x3": 30}
    ]
}
```

### Классификация объекта по готовому алгоритмическому дереву
`POST /api/id3/classify`
```json
{
    "tree": { 
        "type": "node", "feature": "Погода", "branches": { "Солнечно": {"type": "leaf", "value": "Нет"}, "Пасмурно": {"type": "leaf", "value": "Да"} }
    },
    "object": {
        "Погода": "Солнечно",
        "Температура": "Жарко"
    }
}
```